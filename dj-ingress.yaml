apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: dj-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: "todo.com"
    http:
    paths:
    - path: /
      backend:
        serviceName: dj-svc
        servicePort: 30333