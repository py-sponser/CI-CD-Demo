apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dj-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
  - host: "todo.com"
    http:
    paths:
    - path: /
      backend:
        serviceName: dj-svc
        servicePort: 30333